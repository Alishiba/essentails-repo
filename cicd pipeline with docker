EX5: CI Pipeline: Python App → GitHub → Docker Hub
1.
app.py
print("Hello CI Pipeline from Beereddy!")
2.
Dockerfile
FROM python:3.11
WORKDIR /app
COPY . .
CMD ["python", "app.py"]

  Step 4 — Build and test locally
docker build -t Alishiba/demo:local .
docker run Alishiba/demo:local

  Step 5 — Login and push manually (optional)
docker login
docker push Alishiba/demo:local
mkdir .github/workflows/main.yml
name: Docker Build & Push
on: [push]
jobs:
 build:
 runs-on: ubuntu-latest
 steps:
 - uses: actions/checkout@v4
 - uses: docker/login-action@v3
 with:
 username: ${{ secrets.DOCKERHUB_USERNAME }}
 password: ${{ secrets.DOCKERHUB_TOKEN }
 - uses: docker/build-push-action@v6
 with:
 push: true
 tags: Alishiba/demo:latest


next step

Initialize git and commit
git init
git add .
git commit -m "initial commit"
git branch -M main
git remote add origin https://github.com/Alishiba/demo.git
git push -u origin main



Step 9 — Add GitHub Secrets
Go to:
GitHub → demo repo → Settings → Secrets → Actions
Add two secrets:
Secret Name Value
DOCKERHUB_USERNAME beereddy2004
DOCKERHUB_TOKEN
(create token in Docker Hub → Account Settings → Security →
New Access Token)




Step 10 — Done
Push any code change:
git add .
git commit -m "update"
git push


docker build -t Alishiba/demo:local .
docker push  Alishiba/demo:local
